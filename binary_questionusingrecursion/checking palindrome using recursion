bool reverse(  string arr, int s, int len){
       if(s>len/2){
           return true ;
       }
       if (arr[s]!= arr[len-s-1]) {
           return false;
       };
       
        return reverse(arr, ++s, len);
   }
